# PRD: 已回复帖子导航功能

## 📋 需求概述

在 X 时间线页面生成回复后，用户希望能够快速定位到已生成回复的帖子，通过类似音频播放器的导航方式（上一条、开始/暂停、下一条）在已回复的帖子之间跳转。

## 🎯 核心目标

1. **快速定位**：能够快速跳转到已生成回复的帖子
2. **自然交互**：模拟人类滚动行为，避免被 X 算法识别为机器人
3. **便捷操作**：支持快捷键操作，提升使用效率
4. **无缝集成**：与现有控制台（浮动面板）无缝集成

## 📊 功能需求

### 1. 导航控制按钮

在控制台（右下角浮动面板）中添加导航区域，包含：

- **上一条** (`◀ 上一条` 或 `⏮` 图标)
  - 跳转到上一个已回复的帖子
  - 如果当前是第一条，循环到最后一条（可选）

- **开始/暂停** (`▶ 开始` / `⏸ 暂停` 或 `▶` / `⏸` 图标)
  - 开始自动播放模式：按时间顺序自动跳转到下一个已回复的帖子
  - 暂停自动播放模式
  - 自动播放间隔：3-5秒（可配置，使用正态分布随机延迟）

- **下一条** (`下一条 ▶` 或 `⏭` 图标)
  - 跳转到下一个已回复的帖子
  - 如果当前是最后一条，循环到第一条（可选）

### 2. 当前帖子指示器

显示当前浏览的帖子信息：
- 当前序号：`1 / 10`（当前第1条，共10条）
- 帖子预览：显示帖子内容的前30-50个字符（可选）
- 作者信息：显示 `@handle`（可选）

### 3. 帖子列表管理

#### 3.1 数据源
- 从 `chrome.storage.local` 的 `xcomment_batch_completed` 读取已完成任务
- 从内存中的 `tasks` 数组筛选 `status === 'done' || status === 'accepted'` 的任务
- 合并两个数据源，去重（基于 `tweetId`）

#### 3.2 排序规则
- **默认排序**：按回复时间倒序（最新回复的在前面）
- **可选排序**：
  - 按帖子发布时间倒序
  - 按浏览量降序
  - 按作者 handle 字母序

#### 3.3 过滤选项（可选）
- 仅显示今天回复的
- 仅显示最近7天回复的
- 仅显示特定作者回复的

### 4. 智能滚动定位（保守安全策略）

#### 4.1 滚动策略 - 渐进式滚动（唯一方案）
**⚠️ 安全第一原则**：只使用渐进式滚动，完全避免 `scrollIntoView`，模拟真实人类滚轮行为。

1. **渐进式滚动实现**（强制使用）：
   - ❌ **禁止使用** `scrollIntoView`（容易被识别）
   - ✅ **使用** `window.scrollBy()` 模拟滚轮事件
   - ✅ **分多步滚动**：根据距离动态计算步数（5-10步，距离越远步数越多）
   - ✅ **每步随机延迟**：150-400ms（正态分布，均值 250ms，标准差 60ms）
   - ✅ **每步滚动距离随机化**：使用正态分布，避免完全匀速
   - ✅ **滚动速度变化**：模拟人类加速-减速-加速的模式

2. **滚动前准备**（增加真实性）：
   - 滚动前随机延迟：200-500ms（模拟人类反应时间）
   - 检查页面是否在滚动中，如果正在滚动则等待完成
   - 检查用户是否正在交互，如果用户正在操作则延迟执行

3. **滚动过程优化**：
   - **加速阶段**（前30%）：滚动速度逐渐加快
   - **匀速阶段**（中40%）：保持相对稳定速度（带小幅随机波动）
   - **减速阶段**（后30%）：滚动速度逐渐减慢，接近目标时更慢
   - **每步之间**：添加随机停顿（50-200ms），模拟人类阅读

4. **滚动后微调**（重要安全措施）：
   - 滚动完成后，等待 300-600ms（模拟人类定位时间）
   - 添加小幅度随机偏移（±15px，不超过±30px）
   - 模拟人类阅读时的微调行为（1-2次小幅滚动）
   - 最终位置可能不完全居中，更接近人类行为

5. **安全限制**：
   - 单次滚动最大距离：不超过 3 个屏幕高度
   - 如果目标距离超过限制，分多次导航（每次导航后等待 2-5 秒）
   - 连续导航间隔：最少 2 秒（手动）或 4 秒（自动播放）

#### 4.2 元素定位
- 优先使用 `tweetId` 通过 `findArticleByTweetId()` 定位
- 如果找不到，尝试通过 `authorHandle` + `content` 片段匹配
- 如果仍找不到，提示用户"帖子可能已不在当前页面，请刷新页面"

#### 4.3 高亮显示
- 定位到帖子后，添加临时高亮效果（2-3秒后消失）
- 使用 CSS 动画：淡入淡出的边框或背景色变化
- 避免过于显眼，使用柔和的颜色（如淡蓝色边框）

### 5. 快捷键支持

#### 5.1 快捷键映射
- **`←` 或 `J`**：上一条
- **`空格` 或 `K`**：开始/暂停自动播放
- **`→` 或 `L`**：下一条
- **`Esc`**：取消高亮/退出导航模式（可选）

#### 5.2 快捷键作用域
- 仅在 X 页面（`x.com` 或 `pro.x.com`）生效
- 仅在控制台可见时生效（避免与其他页面快捷键冲突）
- 支持在输入框聚焦时禁用（避免输入冲突）

#### 5.3 快捷键提示
- 在控制台导航区域显示快捷键提示（小字，可选隐藏）
- 首次使用时显示提示（可通过设置关闭）

### 6. 自动播放模式

#### 6.1 播放逻辑（保守策略）
- 按排序顺序自动跳转到下一个已回复的帖子
- **每个帖子停留时间**：5-8 秒（正态分布随机：均值 6.5 秒，标准差 0.8 秒）
  - ⚠️ **保守设置**：最少 5 秒，避免过快切换被识别
  - 停留时间包括：滚动时间 + 阅读时间 + 缓冲时间
- **滚动时间**：1.5-3 秒（渐进式滚动）
- **阅读时间**：2-4 秒（模拟人类阅读）
- **缓冲时间**：1-2 秒（安全缓冲）
- 到达最后一条后，可选择：
  - 停止播放（默认）
  - 循环播放（回到第一条，需用户明确启用）

#### 6.2 播放控制（安全限制）
- 播放过程中可以手动点击"上一条"/"下一条"切换
- 点击"暂停"立即停止自动播放
- 播放状态在控制台显示（如"自动播放中..."）
- **安全限制**：
  - 连续自动播放最多 20 条，然后自动暂停（需用户重新启动）
  - 自动播放总时长限制：最多 5 分钟，然后自动暂停
  - 如果检测到用户手动滚动，自动暂停播放（避免冲突）

#### 6.3 播放指示
- 在控制台显示当前播放状态
- 显示倒计时（如"3秒后跳转"）

## 🎨 UI/UX 设计

### 1. 控制台布局调整

在现有控制台底部（状态栏上方）添加导航区域：

```
┌─────────────────────────────┐
│ 控制台                      │
├─────────────────────────────┤
│ [情绪选择区域]              │
├─────────────────────────────┤
│ [开始] [停止] [刷新]        │
├─────────────────────────────┤
│ ┌─────────────────────────┐ │
│ │ ◀ 上一条  │ 1/10 │ 下一条 ▶ │ │  ← 新增导航区域
│ │ [▶ 开始] / [⏸ 暂停]      │ │
│ └─────────────────────────┘ │
├─────────────────────────────┤
│ 待0 / 进行0 / 成功1 / 失败0 │
│ 设置                        │
└─────────────────────────────┘
```

### 2. 导航区域样式

- **布局**：水平排列，紧凑设计
- **按钮样式**：与现有控制台按钮风格一致
- **当前指示器**：显示为小字，居中
- **快捷键提示**：灰色小字，可选显示/隐藏

### 3. 高亮效果

- **颜色**：淡蓝色边框（`#1DA1F2` 透明度 30%）
- **动画**：2 秒淡入淡出
- **位置**：围绕整个 `article` 元素

## 🔧 技术实现

### 1. 数据管理

```javascript
// 导航状态
let navigationState = {
  currentIndex: -1,        // 当前浏览的帖子索引
  completedPosts: [],      // 已回复帖子列表（排序后）
  isAutoPlaying: false,    // 是否自动播放
  autoPlayTimer: null,     // 自动播放定时器
  sortOrder: 'replyTime'   // 排序方式
};

// 获取已回复帖子列表
async function getCompletedPosts() {
  // 1. 从内存 tasks 获取
  const memoryTasks = tasks.filter(t => 
    t.status === 'done' || t.status === 'accepted'
  );
  
  // 2. 从 storage 获取
  const storageData = await chrome.storage.local.get(['xcomment_batch_completed']);
  const storedIds = storageData.xcomment_batch_completed?.ids || [];
  
  // 3. 合并去重
  const postMap = new Map();
  
  // 添加内存中的任务
  memoryTasks.forEach(task => {
    if (task.tweetId) {
      postMap.set(task.tweetId, {
        tweetId: task.tweetId,
        authorHandle: task.authorHandle,
        content: task.content,
        replyTime: task.completedAt || Date.now(),
        postTime: task.postTime,
        views: task.views
      });
    }
  });
  
  // 添加 storage 中的任务（如果内存中没有）
  // 注意：storage 中只有 ID，需要从当前页面的 article 中提取信息
  
  return Array.from(postMap.values());
}
```

### 2. 渐进式滚动实现（保守安全版本）

```javascript
// 正态分布随机数生成（复用现有函数）
function normalRandom(mean, stdDev) {
  const u1 = Math.random();
  const u2 = Math.random();
  const z0 = Math.sqrt(-2 * Math.log(u1)) * Math.cos(2 * Math.PI * u2);
  return Math.max(0, mean + z0 * stdDev);
}

// 检查页面是否正在滚动
function isScrolling() {
  return window._lastScrollTop !== undefined && 
         Math.abs(window.scrollY - window._lastScrollTop) > 5;
}

// 等待滚动停止
async function waitForScrollStop(timeout = 2000) {
  return new Promise((resolve) => {
    let lastScrollY = window.scrollY;
    let scrollTimeout;
    
    const checkScroll = () => {
      const currentScrollY = window.scrollY;
      if (Math.abs(currentScrollY - lastScrollY) < 2) {
        clearTimeout(scrollTimeout);
        resolve();
      } else {
        lastScrollY = currentScrollY;
        scrollTimeout = setTimeout(checkScroll, 100);
      }
    };
    
    scrollTimeout = setTimeout(() => {
      resolve(); // 超时也继续
    }, timeout);
    
    checkScroll();
  });
}

// 渐进式滚动到目标元素（保守安全版本）
async function progressiveScrollToElement(element) {
  if (!element) return false;
  
  // 1. 滚动前检查
  // 如果页面正在滚动，等待完成
  if (isScrolling()) {
    await waitForScrollStop();
    await new Promise(resolve => setTimeout(resolve, normalRandom(300, 100)));
  }
  
  // 2. 滚动前随机延迟（模拟人类反应时间）
  await new Promise(resolve => 
    setTimeout(resolve, normalRandom(350, 100)) // 200-500ms
  );
  
  // 3. 计算目标位置
  const targetRect = element.getBoundingClientRect();
  const currentScrollY = window.scrollY;
  const viewportHeight = window.innerHeight;
  
  // 目标位置：元素中心对齐到视口中心（带随机偏移）
  const targetCenter = currentScrollY + targetRect.top + targetRect.height / 2;
  const viewportCenter = currentScrollY + viewportHeight / 2;
  const distance = targetCenter - viewportCenter;
  
  // 4. 如果距离很小（< 50px），使用小幅微调
  if (Math.abs(distance) < 50) {
    const microAdjust = distance + normalRandom(0, 15) * (Math.random() > 0.5 ? 1 : -1);
    window.scrollBy({ top: microAdjust, behavior: 'smooth' });
    await new Promise(resolve => setTimeout(resolve, normalRandom(400, 100)));
    return true;
  }
  
  // 5. 安全限制：单次滚动不超过 3 个屏幕高度
  const MAX_SCROLL_DISTANCE = viewportHeight * 3;
  if (Math.abs(distance) > MAX_SCROLL_DISTANCE) {
    // 距离太远，分多次导航（先滚动一部分）
    const partialDistance = Math.sign(distance) * MAX_SCROLL_DISTANCE * 0.8;
    await progressiveScrollByDistance(partialDistance);
    // 等待后再继续（让用户有机会看到内容）
    await new Promise(resolve => setTimeout(resolve, normalRandom(3000, 1000)));
    // 递归继续滚动剩余距离
    return await progressiveScrollToElement(element);
  }
  
  // 6. 渐进式滚动（分多步）
  return await progressiveScrollByDistance(distance);
}

// 渐进式滚动指定距离（核心算法）
async function progressiveScrollByDistance(distance) {
  const absDistance = Math.abs(distance);
  const direction = Math.sign(distance);
  
  // 根据距离动态计算步数（5-10步，距离越远步数越多）
  const minSteps = 5;
  const maxSteps = 10;
  const steps = Math.min(
    maxSteps,
    Math.max(minSteps, Math.ceil(absDistance / (window.innerHeight * 0.3)))
  );
  
  const stepDistance = absDistance / steps;
  
  // 滚动阶段：加速-匀速-减速
  for (let i = 0; i < steps; i++) {
    const progress = i / (steps - 1); // 0 到 1
    
    // 计算当前步骤的滚动距离（带加速-减速曲线）
    let stepSize;
    if (progress < 0.3) {
      // 加速阶段（前30%）
      const accelProgress = progress / 0.3;
      stepSize = stepDistance * (0.5 + accelProgress * 0.5); // 0.5x 到 1.0x
    } else if (progress < 0.7) {
      // 匀速阶段（中40%）
      stepSize = stepDistance * (1.0 + normalRandom(0, 0.1)); // 1.0x ± 10%
    } else {
      // 减速阶段（后30%）
      const decelProgress = (progress - 0.7) / 0.3;
      stepSize = stepDistance * (1.0 - decelProgress * 0.5); // 1.0x 到 0.5x
    }
    
    // 添加随机波动（±5%）
    stepSize *= (1 + normalRandom(0, 0.05) * (Math.random() > 0.5 ? 1 : -1));
    
    // 执行滚动
    window.scrollBy({
      top: stepSize * direction,
      behavior: 'smooth'
    });
    
    // 步骤间延迟（150-400ms，正态分布）
    const stepDelay = normalRandom(250, 60);
    await new Promise(resolve => setTimeout(resolve, stepDelay));
    
    // 随机停顿（模拟阅读，10%概率）
    if (Math.random() < 0.1) {
      await new Promise(resolve => 
        setTimeout(resolve, normalRandom(200, 80)) // 50-200ms
      );
    }
  }
  
  // 7. 滚动后微调
  await waitForScrollStop(1500);
  await new Promise(resolve => setTimeout(resolve, normalRandom(450, 100))); // 300-600ms
  
  // 小幅随机偏移（±15px）
  const microAdjust = normalRandom(0, 8) * (Math.random() > 0.5 ? 1 : -1);
  if (Math.abs(microAdjust) > 5) {
    window.scrollBy({ top: microAdjust, behavior: 'smooth' });
    await new Promise(resolve => setTimeout(resolve, normalRandom(300, 100)));
  }
  
  // 可能的二次微调（30%概率，模拟人类调整）
  if (Math.random() < 0.3) {
    await new Promise(resolve => setTimeout(resolve, normalRandom(200, 100)));
    const secondAdjust = normalRandom(0, 5) * (Math.random() > 0.5 ? 1 : -1);
    if (Math.abs(secondAdjust) > 2) {
      window.scrollBy({ top: secondAdjust, behavior: 'smooth' });
      await new Promise(resolve => setTimeout(resolve, normalRandom(200, 50)));
    }
  }
  
  return true;
}
```

### 3. 快捷键处理

```javascript
// 快捷键监听
function setupKeyboardShortcuts() {
  document.addEventListener('keydown', (e) => {
    // 检查是否在 X 页面
    if (!window.location.hostname.includes('x.com')) return;
    
    // 检查是否在输入框中
    const activeElement = document.activeElement;
    if (activeElement && (
      activeElement.tagName === 'INPUT' ||
      activeElement.tagName === 'TEXTAREA' ||
      activeElement.isContentEditable
    )) {
      return; // 输入时禁用快捷键
    }
    
    // 检查控制台是否可见
    const panel = document.getElementById(PANEL_ID);
    if (!panel || panel.style.display === 'none') return;
    
    // 处理快捷键
    switch(e.key) {
      case 'ArrowLeft':
      case 'j':
      case 'J':
        e.preventDefault();
        navigateToPrevious();
        break;
      case 'ArrowRight':
      case 'l':
      case 'L':
        e.preventDefault();
        navigateToNext();
        break;
      case ' ':
      case 'k':
      case 'K':
        e.preventDefault();
        toggleAutoPlay();
        break;
      case 'Escape':
        e.preventDefault();
        clearHighlight();
        break;
    }
  });
}
```

### 4. 自动播放实现（保守安全版本）

```javascript
// 自动播放状态
let autoPlayState = {
  isPlaying: false,
  timer: null,
  startTime: null,
  playCount: 0,
  maxPlayCount: 20,        // 最多连续播放20条
  maxDuration: 5 * 60 * 1000, // 最多播放5分钟
  userScrollDetected: false
};

// 检测用户手动滚动
let lastUserScrollY = window.scrollY;
let userScrollTimeout = null;

function detectUserScroll() {
  const currentScrollY = window.scrollY;
  const scrollDiff = Math.abs(currentScrollY - lastUserScrollY);
  
  // 如果滚动距离较大（> 50px），可能是用户手动滚动
  if (scrollDiff > 50 && !autoPlayState.isPlaying) {
    // 标记为用户滚动（在自动播放时检测）
    if (autoPlayState.isPlaying) {
      autoPlayState.userScrollDetected = true;
      stopAutoPlay('检测到用户手动滚动，已自动暂停');
    }
  }
  
  lastUserScrollY = currentScrollY;
  
  // 节流检测
  if (userScrollTimeout) clearTimeout(userScrollTimeout);
  userScrollTimeout = setTimeout(detectUserScroll, 500);
}

// 启动用户滚动检测
window.addEventListener('scroll', () => {
  if (!userScrollTimeout) {
    detectUserScroll();
  }
}, { passive: true });

// 自动播放（保守安全版本）
function startAutoPlay() {
  if (autoPlayState.isPlaying) return;
  
  // 重置状态
  autoPlayState.isPlaying = true;
  autoPlayState.startTime = Date.now();
  autoPlayState.playCount = 0;
  autoPlayState.userScrollDetected = false;
  
  updateAutoPlayButton();
  
  const playNext = async () => {
    // 安全检查
    if (!autoPlayState.isPlaying) return;
    
    // 检查播放次数限制
    if (autoPlayState.playCount >= autoPlayState.maxPlayCount) {
      stopAutoPlay(`已连续播放 ${autoPlayState.maxPlayCount} 条，自动暂停`);
      return;
    }
    
    // 检查播放时长限制
    const elapsed = Date.now() - autoPlayState.startTime;
    if (elapsed >= autoPlayState.maxDuration) {
      stopAutoPlay('已播放 5 分钟，自动暂停');
      return;
    }
    
    // 检查用户是否手动滚动
    if (autoPlayState.userScrollDetected) {
      stopAutoPlay('检测到用户手动滚动，已自动暂停');
      return;
    }
    
    // 跳转到下一个
    const success = await navigateToNext();
    
    if (!success) {
      // 没有下一个了，停止播放
      stopAutoPlay('已到达最后一条');
      return;
    }
    
    autoPlayState.playCount++;
    
    // 计算停留时间（保守：5-8秒）
    const scrollTime = normalRandom(2000, 500);      // 滚动时间：1.5-3秒
    const readTime = normalRandom(3000, 800);        // 阅读时间：2-4秒
    const bufferTime = normalRandom(1500, 400);     // 缓冲时间：1-2秒
    const totalDelay = scrollTime + readTime + bufferTime; // 总计：5-8秒
    
    // 更新倒计时显示
    updateCountdown(totalDelay);
    
    // 设置定时器
    autoPlayState.timer = setTimeout(() => {
      playNext();
    }, totalDelay);
  };
  
  // 延迟启动（模拟用户操作）
  setTimeout(() => {
    playNext();
  }, normalRandom(500, 200));
}

function stopAutoPlay(reason = '') {
  autoPlayState.isPlaying = false;
  if (autoPlayState.timer) {
    clearTimeout(autoPlayState.timer);
    autoPlayState.timer = null;
  }
  updateAutoPlayButton();
  clearCountdown();
  
  if (reason) {
    showNotification(reason, 2000);
  }
}
```

## 📝 实现步骤

### Phase 1: 基础导航功能
1. ✅ 在控制台添加导航区域 UI
2. ✅ 实现获取已回复帖子列表
3. ✅ 实现"上一条"/"下一条"跳转
4. ✅ 实现基础滚动定位

### Phase 2: 人类化滚动
1. ✅ 实现渐进式滚动算法
2. ✅ 添加随机延迟和微调
3. ✅ 实现高亮效果

### Phase 3: 自动播放
1. ✅ 实现自动播放逻辑
2. ✅ 添加播放状态显示
3. ✅ 实现倒计时显示

### Phase 4: 快捷键支持
1. ✅ 实现快捷键监听
2. ✅ 添加快捷键提示
3. ✅ 处理输入框冲突

### Phase 5: 优化和测试
1. ✅ 性能优化
2. ✅ 边界情况处理
3. ✅ 用户体验优化

## ⚠️ 风险与注意事项

### 1. 算法识别风险（最高优先级）
- **风险**：频繁的自动滚动可能被 X 算法识别为机器人行为
- **缓解措施**（多层防护）：
  1. **强制渐进式滚动**：完全避免 `scrollIntoView`，只使用 `scrollBy` 模拟滚轮
  2. **保守的时间间隔**：
     - 自动播放间隔：最少 5 秒（均值 6.5 秒）
     - 手动导航间隔：最少 2 秒
     - 滚动前延迟：200-500ms（模拟反应时间）
  3. **随机性和变化**：
     - 滚动步数：5-10 步（动态计算）
     - 每步延迟：150-400ms（正态分布）
     - 滚动速度：加速-匀速-减速曲线
     - 随机停顿：10% 概率模拟阅读
  4. **安全限制**：
     - 连续播放最多 20 条
     - 总播放时长最多 5 分钟
     - 单次滚动距离不超过 3 个屏幕高度
     - 检测用户手动滚动时自动暂停
  5. **人类行为模拟**：
     - 滚动后微调（±15px）
     - 可能的二次微调（30% 概率）
     - 等待滚动完成后再继续
     - 检查页面状态（是否正在滚动、用户是否在交互）

### 2. 性能风险
- **风险**：大量帖子列表可能影响性能
- **缓解**：
  - 限制列表大小（最多保留最近100条）
  - 使用虚拟滚动（如果列表很长）
  - 延迟加载帖子详情
  - 滚动过程中暂停其他监控（如徽章监控）

### 2.1 滚动性能优化
- 使用 `requestAnimationFrame` 优化滚动动画
- 滚动过程中暂停 MutationObserver
- 滚动完成后延迟恢复监控（500-1000ms）

### 3. DOM 变化风险
- **风险**：X 平台 DOM 结构变化可能导致定位失败
- **缓解**：
  - 使用多种定位方式（tweetId、content 匹配）
  - 添加降级方案（提示用户刷新页面）
  - 定期检查并更新选择器

### 4. 快捷键冲突
- **风险**：可能与 X 平台或其他扩展的快捷键冲突
- **缓解**：
  - 仅在控制台可见时生效
  - 在输入框聚焦时禁用
  - 提供快捷键自定义选项（可选）

## 🎯 成功指标

1. **功能完整性**：能够成功定位并跳转到所有已回复的帖子
2. **用户体验**：导航操作流畅，无明显延迟（滚动时间 1.5-3 秒可接受）
3. **安全性**（最高优先级）：
   - 使用 2 周后，未触发 X 平台异常检测
   - 滚动行为通过人工审查（看起来像人类操作）
   - 自动播放限制有效（不会无限播放）
4. **使用率**：至少 50% 的用户使用过导航功能
5. **稳定性**：滚动成功率 > 95%（帖子在当前页面时）

## 🔒 安全配置选项（可选）

在设置页面添加导航安全配置：

1. **滚动速度**：
   - 保守（默认）：5-8 秒间隔，5-10 步滚动
   - 标准：4-6 秒间隔，4-8 步滚动
   - 快速（不推荐）：3-5 秒间隔，3-6 步滚动

2. **自动播放限制**：
   - 连续播放上限：10 / 20 / 30 条（默认 20）
   - 总时长上限：3 / 5 / 10 分钟（默认 5 分钟）

3. **安全检测**：
   - 启用用户滚动检测（默认开启）
   - 启用播放次数限制（默认开启）
   - 启用播放时长限制（默认开启）

## 📅 时间估算

- **Phase 1**：2-3 天
- **Phase 2**：1-2 天
- **Phase 3**：1-2 天
- **Phase 4**：1 天
- **Phase 5**：1-2 天

**总计**：6-10 天

## 🔄 后续优化

1. **智能推荐**：根据用户浏览历史推荐相关帖子
2. **批量操作**：支持批量查看、删除已回复帖子
3. **导出功能**：导出已回复帖子列表
4. **统计分析**：显示回复效果统计（点赞、转发等）
5. **自定义排序**：支持用户自定义排序规则

---

**PRD 版本**：v1.1（保守安全版）  
**创建日期**：2026-01-19  
**更新日期**：2026-01-19  
**作者**：Claude (AI Assistant)

## 📝 版本更新记录

### v1.1 (2026-01-19)
- ✅ **强制使用渐进式滚动**：完全移除 `scrollIntoView`，只使用 `scrollBy` 模拟滚轮
- ✅ **保守的时间设置**：自动播放间隔从 3-5 秒提升到 5-8 秒
- ✅ **增强的安全限制**：
  - 连续播放最多 20 条
  - 总播放时长最多 5 分钟
  - 单次滚动距离限制（3 个屏幕高度）
  - 用户滚动检测自动暂停
- ✅ **更真实的滚动行为**：
  - 加速-匀速-减速曲线
  - 随机停顿模拟阅读
  - 滚动后微调（±15px）
  - 可能的二次微调（30% 概率）
- ✅ **滚动前安全检查**：
  - 检查页面是否正在滚动
  - 等待滚动完成
  - 滚动前随机延迟（200-500ms）
